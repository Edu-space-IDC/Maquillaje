
  # Read the file

  This is a code bundle for Read the file. The original project is available at https://www.figma.com/design/Pj1qEZMNzkyOsahVFE2EiQ/Read-the-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # Maquillaje
